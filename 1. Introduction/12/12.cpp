#include <iostream>
using std::cout;

int main()
{
    const int x = 5; // Read-only constant variable (i.e. immutable)

    enum {y = 100}; // This is another type of constant, used restrict a variable to a defined set of values


    return 0;
}